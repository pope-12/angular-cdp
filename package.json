{
  "name": "angular",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node_modules/.bin/json-server-auth --watch db.json --delay=300 --snapshots=snapshots -r routes.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "json-server": "^0.15.0",
    "json-server-auth": "^1.2.1"
  }
}
